#!/bin/bash -e

# If running the rails server then migrate existing database
# Using db:migrate instead of db:prepare to preserve existing data
if [ "${@: -2:1}" == "./bin/rails" ] && [ "${@: -1:1}" == "server" ]; then
  ./bin/rails db:migrate
fi

exec "${@}"
