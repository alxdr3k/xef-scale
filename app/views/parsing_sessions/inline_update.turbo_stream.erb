<%= turbo_stream.replace dom_id(@parsing_session) do %>
  <%= render 'parsing_session_row', parsing_session: @parsing_session, workspace: @workspace, open_issue_count: @parsing_session.open_import_issues.size %>
<% end %>
