<%= turbo_stream.update "flash", partial: "shared/flash" %>
<%= turbo_stream.replace dom_id(@transaction),
    partial: "reviews/transaction_row",
    locals: {
      transaction: @transaction,
      categories: @workspace.categories.order(:name),
      read_only: false
    } %>
