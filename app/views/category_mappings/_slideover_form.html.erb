<turbo-frame id="slideover-content">
  <h2 class="text-lg font-semibold text-gray-900 mb-6"><%= @mapping.persisted? ? '규칙 수정' : '새 규칙' %></h2>

  <%= form_with model: [@workspace, @mapping],
        url: @mapping.persisted? ? workspace_category_mapping_path(@workspace, @mapping) : workspace_category_mappings_path(@workspace),
        class: 'space-y-4',
        data: { turbo_frame: '_top' } do |f| %>

    <% if @mapping.errors.any? %>
      <div class="bg-red-50 border border-red-200 rounded-md p-4">
        <ul class="text-sm text-red-700 list-disc list-inside">
          <% @mapping.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div>
      <label class="block text-sm font-medium text-gray-700 mb-1">내역 패턴 *</label>
      <%= f.text_field :merchant_pattern,
            class: 'block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500',
            placeholder: '예: 스타벅스, 배달의민족',
            required: true,
            autofocus: true %>
    </div>

    <div>
      <label class="block text-sm font-medium text-gray-700 mb-1">매칭 방식</label>
      <%= f.select :match_type,
            [["정확히 일치", "exact"], ["포함", "contains"]],
            {},
            class: 'block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500' %>
      <p class="mt-1 text-xs text-gray-500">
        "정확히 일치": 가맹점명이 패턴과 동일할 때 매칭<br>
        "포함": 가맹점명에 패턴이 포함될 때 매칭
      </p>
    </div>

    <div>
      <label class="block text-sm font-medium text-gray-700 mb-1">금액 조건</label>
      <%= f.number_field :amount,
            class: 'block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500',
            placeholder: '비워두면 모든 금액에 적용',
            min: 1 %>
      <p class="mt-1 text-xs text-gray-500">특정 금액일 때만 매칭하려면 입력하세요.</p>
    </div>

    <div>
      <label class="block text-sm font-medium text-gray-700 mb-1">카테고리 *</label>
      <%= f.collection_select :category_id, @categories, :id, :name,
            { prompt: "카테고리 선택" },
            { class: 'block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500',
              required: true } %>
    </div>

    <div class="flex justify-end gap-3 pt-4 border-t border-gray-200">
      <button type="button"
              data-action="click->slideover#close"
              class="px-4 py-2 text-gray-700 hover:text-gray-900">
        취소
      </button>
      <%= f.submit '저장', class: 'bg-indigo-600 text-white px-4 py-2 rounded-lg hover:bg-indigo-700 cursor-pointer' %>
    </div>
  <% end %>
</turbo-frame>
